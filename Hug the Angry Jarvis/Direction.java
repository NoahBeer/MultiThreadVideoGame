/**
 * public enum Direction creates 8 constants to be used throughout lab 5. These constants serve as directions, specifying which way each 
 * Boardable object should move. The 8 constants are UP, DOWN, LEFT, RIGHT, UP_LEFT, UP_RIGHT, DOWN_LEFT, DOWN_RIGHT
 * 
 * @author Noah Beer
 */
public enum Direction {
	
	/**
	 * Each of the possible Directions that Mobile subclasses are able to move
	 */
	UP, DOWN, LEFT, RIGHT, UP_LEFT, UP_RIGHT, DOWN_LEFT, DOWN_RIGHT
} // end enum Direction